# 规划模块

## Planner 节点

```python
async def plan_node(state: AgentState) -> dict:
    # 1. 加载会话历史
    messages = await session_context.load_chat_messages(...)

    # 2. RAG 知识库自动检索 【新增】
    rag_messages = await build_rag_context_for_planner(user_message, settings)
    messages.extend(rag_messages)

    # 3. 追加黑板笔记
    notes = state.get("blackboard_notes") or []
    if notes:
        messages.append(HumanMessage(content=bb))

    # 4. 调用 LLM 生成计划
    steps = await plan_steps_with_llm(messages, settings)

    return {"plan_steps": steps, "replan_count": ...}
```

## RAG 融合

在规划阶段自动检索 RAG 知识库：

```python
# 构建 RAG 上下文
rag_messages = await build_rag_context_for_planner(
    user_message,
    settings=settings,
)
# 注入到消息列表
messages.extend(rag_messages)
```

## 计划结构

```json
{
  "steps": [
    {"id": "1", "title": "理解需求", "description": "..."},
    {"id": "2", "title": "分解任务", "description": "..."}
  ]
}
```

## 重规划

触发条件：
- Actor 请求重规划
- Learner LLM 反思建议重规划
- `replan_count < max_replan_attempts`
