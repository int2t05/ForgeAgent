# 技术难点与解决方案

## 1. 高并发处理

### Q: 如何处理高并发？

**问题**：多个任务同时执行，SSE 轮询可能打爆数据库

**方案**：
```python
# 1. 复用 DB 连接
async with AsyncSessionLocal() as db:
    async for update in graph.astream(stream_input, config):
        # 多个任务共用连接池

# 2. SSE 轮询间隔
_POLL_INTERVAL_SEC = 0.1  # 100ms，不是 10ms

# 3. 限制批量大小
limit=200  # 每次最多拉 200 条
```

---

## 2. Token 上下文超限

### Q: 上下文超出模型限制怎么办？

**三层策略**：

```python
# 1. 精确计数优先
if settings.llm_use_exact_token_count:
    tokens = count_messages_tokens(msgs)  # tiktoken

# 2. 启发式估算兜底
else:
    tokens = len(content) // 3 + msgs * 4

# 3. 截断到预算
if tokens > max_input_tokens:
    msgs = truncate_to_budget(msgs, max_input_tokens)
```

---

## 3. 工具执行超时

### Q: 工具执行卡死怎么办？

**方案**：
```python
# asyncio.timeout 控制超时
async with asyncio.timeout(timeout_sec):
    result = await tool.ainvoke(args)

# 各工具独立超时
TOOL_TIMEOUTS = {
    "shell": 60.0,
    "python_repl": 30.0,
    "search": 10.0,
    "file": 5.0,
}
```

---

## 4. LLM 调用失败

### Q: LLM API 不稳定怎么办？

**方案**：
```python
# 1. 指数退避重试
for attempt in range(max_attempts):
    try:
        return await chat.ainvoke(messages)
    except RateLimitError:
        wait = min(max_delay, base * (2 ** attempt))
        await asyncio.sleep(wait)

# 2. 熔断器保护
breaker = CircuitBreaker(failure_threshold=10)
breaker.before_call()
```

---

## 5. 任务中断恢复

### Q: 服务崩溃如何恢复任务？

**Checkpoint 机制**：
```python
# 1. 保存检查点
graph = builder.compile(checkpointer=AsyncSqliteSaver.from_conn_string(...))

# 2. 检查是否有未完成任务
snap = await graph.aget_state(config)
if snap.next:  # 有未完成节点
    result = await graph.ainvoke(None, config)  # 继续执行

# 3. 状态恢复
state = snap.values
```

---

## 6. 前端渲染性能

### Q: SSE 事件频繁导致卡顿？

**问题**：每个事件触发全局重渲染

**方案**：
```typescript
// 1. 细粒度状态订阅
const llmThinking = useComposerTaskStore(s => s.llmThinking);  // 只订阅这个
const llmAnswer = useComposerTaskStore(s => s.llmAnswer);

// 2. React.memo 优化
const MessageList = React.memo(({ messages }) => (
  messages.map(m => <MessageBubble key={m.id} message={m} />)
));

// 3. 事件批量处理
setLiveEvents: (events) => set((state) => ({
  ...state,
  events: [...state.events, ...events],  // 批量追加
}))
```

---

## 7. 数据库写入瓶颈

### Q: 每次工具调用都写库太慢？

**问题**：11 条事件/步 × 15ms = 165ms

**方案**：
```python
# 批量写入
async def append_events_batch(session, task_id, events):
    stmt = insert(TaskEvent).values([
        {"task_id": task_id, "module": m, "kind": k, "payload_json": p}
        for m, k, p in events
    ])
    await session.execute(stmt)

# 使用
events = [
    ("execution", "tool_call", json.dumps({...})),
    ("execution", "tool_result", json.dumps({...})),
]
await append_events_batch(db, task_id, events)
```

---

## 8. 工具选择优化

### Q: 如何选择最合适的工具？

**当前**：LLM 自行决定

**可优化**：
```python
async def select_best_tool(user_message, available_tools):
    # 1. 关键词匹配
    intent_keywords = extract_keywords(user_message)

    for tool in available_tools:
        score = sum(
            1 for kw in intent_keywords
            if kw in tool.description
        )

    # 2. 返回最高分
    return max(scored_tools, key=lambda x: x[0])
```

---

## 9. 计划验证

### Q: 如何避免生成无效计划？

**方案**：Plan-and-Validate
```python
async def validate_plan(steps):
    issues = []

    # 1. 步骤是否可执行
    for step in steps:
        if not step.get("title"):
            issues.append("缺少步骤标题")

    # 2. 步骤顺序是否合理
    for i in range(len(steps) - 1):
        if steps[i] == steps[i+1]:
            issues.append("重复步骤")

    # 3. 返回验证结果
    return {"is_valid": len(issues) == 0, "issues": issues}
```

---

## 10. 安全风险

### Q: Shell 工具如何防止注入？

**方案**：
```python
# 1. 命令白名单
ALLOWED_COMMANDS = ["ls", "cat", "git", "npm", "pip"]

# 2. 参数清理
def sanitize_shell_args(args):
    dangerous = [";", "|", "&", ">", "<", "`"]
    for d in dangerous:
        if d in args:
            raise ValueError(f"危险字符: {d}")
    return args

# 3. 超时限制
timeout=60  # 最长 60 秒
```

---

## 11. 多租户隔离

### Q: 如何实现多租户隔离？

**当前**：单租户（MVP）

**可扩展**：
```python
# 1. Session 表加 user_id
class Session:
    id = Column(Integer)
    user_id = Column(String)  # 租户 ID
    title = Column(String)

# 2. API 层校验
@router.get("/sessions/{id}")
async def get_session(session_id, current_user):
    session = await session_repository.get_session(session_id)
    if session.user_id != current_user.id:
        raise ForbiddenError()
    return session
```

---

## 12. 监控与可观测性

### Q: 如何监控系统健康？

**方案**：
```python
# 1. 关键指标埋点
metrics = {
    "task_created_to_first_event_ms": elapsed,
    "llm_first_token_ms": elapsed,
    "db_connections_per_task": count,
    "sse_events_per_second": rate,
}

# 2. 错误率监控
error_count = 0
success_count = 0
error_rate = error_count / (error_count + success_count)

# 3. 日志
logger.info(f"Task {task_id} completed in {elapsed}s")
logger.warning(f"Tool {tool_name} timeout")
```

---

## 13. 缓存策略

### Q: 哪些数据适合缓存？

| 数据 | 缓存策略 | TTL |
|------|----------|-----|
| 工具描述 | 启动时加载 | 常驻 |
| 用户设置 | 修改时失效 | 5min |
| 会话列表 | 轮询刷新 | 30s |
| LLM 响应 | 禁用 | - |

---

## 14. 性能瓶颈总结

| 瓶颈 | 影响 | 解决方案 |
|------|------|----------|
| append_event SELECT MAX | DB 查询 | INSERT RETURNING |
| SSE 连接复用 | 连接数 | 单长连接 |
| 工具串行执行 | 延迟 | asyncio.gather |
| 前端重渲染 | CPU 高 | Zustand 细粒度 |
| LLM 重试过长 | 延迟 | 降低退避参数 |
